import React, { FC } from "react";
import NetworkPortfolio from "Portfolio/NetworkPortfolio";

interface RootLayoutProps {}

const RootLayout: FC<RootLayoutProps> = () => {
  return <NetworkPortfolio />;
};

export default RootLayout;
